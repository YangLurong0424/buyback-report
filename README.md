# Buyback Report

This repository publishes a static daily buyback overview page.

The public page is served from `docs/index.html`.

Sensitive local files are intentionally excluded from version control, including:

- `mail_imap_config.json`
- `mail_downloads/`
- `outputs/`

